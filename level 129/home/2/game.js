import mainCharacter from './characters.js';
import { warrior as tank } from './characters.js';
import { healer as support } from './characters.js';

console.log(`Party: ${mainCharacter} (Main), ${tank} (Tank), ${support} (Support)`);
